{
  "name": "cutroom-worker",
  "version": "1.0.0",
  "description": "FFmpeg worker for Cutroom — renders cleaned videos and clips, uploads to Lovable Cloud (Supabase) Storage.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "express": "^4.19.2",
    "fluent-ffmpeg": "^2.1.3"
  },
  "engines": { "node": ">=18" }
}
